{{define "base.gohtml"}}
{{ template "header.gohtml" . }}

<header class="max-w-4xl mx-auto px-4 flex gap-3 items-center my-4">
  <div class="w-10 h-10 rounded-xl bg-pink-600 flex items-center justify-center text-white text-2xl font-bold">🍦</div>
  <div>
    <h1 class="text-2xl font-medium m-0">{{if .PageTitle}}{{.PageTitle}}{{else}}Panel de Satisfacción{{end}}</h1>
    <div class="text-xs text-gray-500">Go + HTMX + Gorm (Docker)</div>
  </div>
</header>
<main class="max-w-4xl mx-auto px-4">
  {{block "content" .}}{{end}}
</main>
{{template "footer.gohtml" .}}
</body>
</html>
{{end}}
