version: '3.8'

services:
  app:
    build:
      context: .
      target: build  # Stop at build stage
    volumes:
      - .:/app
      - go-mod-cache:/go/pkg/mod
      - go-build-cache:/root/.cache/go-build
    command: go run ./cmd/server
    # ...existing code from docker-compose.yml...

volumes:
  go-mod-cache:
  go-build-cache:
